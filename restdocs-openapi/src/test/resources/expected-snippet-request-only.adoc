/some/{id}:
  post:
    description: some resource
    parameters:
      - in: path
        name: id
        schema:
          type: integer
    responses:
      200:
        description: some response
        content:
          application/json:
            examples:
              example0:
                $ref: 'test-request.json'
            schema:
              $ref: 'test-schema-request.json'
