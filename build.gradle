plugins {
    id 'pl.allegro.tech.build.axion-release' version '1.9.2'
}

repositories {
    jcenter()
}

scmVersion {
    tag {
        prefix = ''
    }
}

allprojects {
    project.version = scmVersion.version
}
